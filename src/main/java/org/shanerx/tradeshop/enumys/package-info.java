/**
 * This package contains all the enum types the plugin's internal API makes use of.
 * We are including them into the javadocs so that you can have a full picture of the
 * plugin's API, since the TSAF makes use of it.
 */
package org.shanerx.tradeshop.enumys;